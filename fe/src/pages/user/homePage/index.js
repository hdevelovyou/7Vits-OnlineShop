import { memo } from "react";
import "./style.scss";
const Homepage = () => {
    return (
        <div id="content">
            <div className="banner">

            </div>
        </div>
    );
}

export default memo(Homepage);
